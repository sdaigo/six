import { css } from "@/styled-system/css";

export const footer = css({
  borderTop: "1px solid {colors.border.emphasized}",
  py: 10,
  bg: "bg.emphasized",
});

export const container = css({});

export const copyright = css({
  textStyle: "caption",
  color: "foreground.muted",
});
